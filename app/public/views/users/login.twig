{% set field = "w3-input w3-border w3-margin-bottom" %}

{% extends "layout.twig" %}
    
{% block content %}
    <h2>Connexion</h2>

    {% if 1 %}
        <form action="{{ root }}/login" class="w3-container w3-third" method="post">
            {# Adresse e-mail #}

            <label for="email">Adresse e-mail</label>
            <input type="text" name="email" id="email" value="{{ email }}" class="{{ field }}">

            <p class="w3-text-red">{{ errors.email }}</p>

            {# Mot de passe #}

            <label for="password">Mot de passe</label>
            <input type="password" name="password" id="password" value="{{ password }}" class="{{ field }}">

            <p class="w3-text-red">{{ errors.password }}</p>

            {# Jeton CSRF #}

            <input type="hidden" name="token" value="{{ token }}">

            {# Bouton #}

            <button class="w3-btn w3-margin-bottom w3-blue">Se connecter</button>

            {# Lien d'inscription #}

            <p>
                <a class="w3-text-blue" href="{{ root }}/register">Pas encore inscrit(e) ?</a>
            </p>

            {#
            <p>
                <a class="w3-text-blue" href="{{ root }}/reset">Mot de passe oublié ?</a>
            </p>
            #}
        </form>
    {% else %}
        <p>Vous êtes déjà connecté(e).</p>

        <p>Veuillez revenir sur la <a class="w3-text-blue" href="{{ root }}">page d'accueil</a>.</p>
    {% endif %}
{% endblock %}