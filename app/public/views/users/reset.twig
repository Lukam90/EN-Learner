{% extends "layout.twig" %}

{% block content %}
    <h2>{{ pageTitle }}</h2>

    <form action="{{ root }}/users/reset" class="third" method="post">
        {# Adresse e-mail #}

        <label for="email">Adresse e-mail</label>
        <input type="text" name="email" id="email" value="{{ email }}" class="input">

        <p class="error">{{ errors.email }}</p>

        {# Bouton #}

        <button class="btn-info m-bottom">Envoyer</button>

        {# Lien de connexion #}

        <p>
            <a href="{{ root }}/users/login">&lt; Revenir à la page de connexion</a>
        </p>
    </form>
{% endblock %}